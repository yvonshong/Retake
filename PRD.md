# Retake - 圣地巡礼拍照 App

## 产品概述

Retake 是一款专为"圣地巡礼"场景设计的 iOS 拍照应用。用户可以选择一张参考图片（如动漫截图、旧照片），将其半透明叠加在相机实时取景画面上，通过调节透明度来精准对齐场景角度，最终拍摄出与原图高度吻合的照片。

## 目标用户

- 动漫圣地巡礼爱好者
- 旅行摄影爱好者
- 需要在相同地点复刻旧照片的用户

## 核心功能

### 1. 选择参考图片
- 从系统相册中选择一张图片作为参考叠加图
- 支持常见图片格式（JPEG、PNG、HEIC）

### 2. 相机取景 + 图层叠加
- 打开系统相机实时取景画面（全屏）
- 用户选择的参考图以半透明方式覆盖在相机画面最上层
- 参考图自动适配屏幕比例（保持宽高比，居中显示）

### 3. 透明度调节
- 屏幕上方水平放置一个透明度调节滑块（Slider）
- 滑块控制叠加图层的不透明度（0% ~ 100%）
- 默认值：**37%**（适合对齐场景的最佳平衡点）
- 实时预览透明度变化

### 4. 拍照
- 提供拍照按钮，点击后捕获当前相机画面（不含叠加图层）
- 拍摄的照片自动保存到系统相册

## 用户流程

```
启动 App
  → 首页：显示「选择参考图片」按钮
  → 用户点击按钮，弹出系统相册选择器
  → 选择图片后，自动进入相机拍照界面
  → 相机界面：
      - 底层：相机实时取景
      - 顶层：半透明参考图（默认 37% 不透明度）
      - 上方：透明度调节滑块
      - 下方：拍照按钮
  → 用户调节透明度、移动手机对齐场景
  → 点击拍照按钮 → 保存照片到相册
  → 可继续拍照或返回首页重新选择图片
```

## 技术方案

| 模块 | 技术选型 |
|------|---------|
| UI 框架 | SwiftUI |
| 相机 | AVFoundation (AVCaptureSession) |
| 相册选择 | PhotosUI (PHPickerViewController) |
| 照片保存 | Photos framework |
| 最低系统版本 | iOS 16.0 |

## 权限需求

- `NSCameraUsageDescription` - 需要使用相机进行拍照
- `NSPhotoLibraryUsageDescription` - 需要访问相册选择参考图片并保存照片

## 界面设计

### 首页
- 简洁设计，居中显示 App 名称 + 选择图片按钮
- 支持 Dark Mode

### 相机叠加页
```
┌─────────────────────────┐
│  ◀ 返回    [===●===] 37%│  ← 透明度滑块
│                         │
│                         │
│    相机实时画面          │
│    +                    │
│    半透明参考图叠加      │
│                         │
│                         │
│         [ ◉ ]           │  ← 拍照按钮
└─────────────────────────┘
```

## MVP 范围

- [x] 图片选择
- [x] 相机取景
- [x] 图层叠加
- [x] 透明度调节（默认 37%）
- [x] 拍照保存

## 后续迭代（暂不实现）

- 叠加图支持双指缩放/旋转/平移
- 拍摄前后对比视图
- 图片收藏夹 / 历史记录
- 分享功能
